# 애드센스 재도전 진행 기록 (인수인계)

> **목적**: 구글 애드센스 "가치가 별로 없는 콘텐츠" **4차 거절** 대응.
> 세션이 끊겨도 이 문서만 읽으면 이어서 작업할 수 있도록 유지한다.
> 상세 계획 원본: `C:\Users\swims\.claude\plans\tistory-elegant-liskov.md`

**최종 갱신**: 2026-08-17 · **현재 단계**: Phase 0~5 전부 완료.
`<meta name="author">` 스킨 편집(사용자 직접) 반영 확인(`content="머니브리핑"`, 홈·개별글 모두 200 확인)
후 **2026-08-17 5차 애드센스 검토 요청 제출 완료.** 결과 대기 중 (구글 심사 통상 수일~수주 소요).
(컴플라이언스 사각지대 13건 + 중복 24건 + **규칙 리터럴 획일성 113개 전량** 수정 완료)

---

## 1. 상황 요약

j2gblog.tistory.com이 애드센스에서 **4번 연속** 같은 사유로 거절됐다.
과거 3번의 대응(`_workspace/adsense_batch1/`, `adsense_batch2/` — 개별 포스트 15개 사후 재작성)은
전부 실패했다.

### 핵심 진단 — 왜 계속 실패했나

**획일성은 규칙 위반이 아니라 규칙 준수의 결과다.**
`.claude/agents/blog-writer.md`가 획일성을 직접 생산하고 있어서, 개별 글을 고쳐도
새 글마다 같은 패턴이 재생산된다. 이걸 안 고치면 5차도 실패한다.

| 원인 | 근거(실측) |
|---|---|
| `## 마무리`가 산출물 포맷에 **리터럴로** 박힘 | 초안 34개 중 **30개(88%)**가 동일, 변형 0종 |
| 규칙 5가 체크리스트를 전 포스트 의무화 | "체크리스트" 단어 **33/34(97%)** |
| 규칙 3 제목 템플릿이 "확인할 N가지"를 정답 예시로 제시 | 제목에 "확인할" **36/108(33%)** |
| **분량 최소 기준 규칙이 아예 없음** | 본문 평균 2,410자, H2 7~10개 → 섹션당 ~250자 |
| desc 종결 표현 획일 | "정리했습니다" 종결 **72/108(66.7%)** |
| E-E-A-T 규칙 0건 | YMYL 금융인데 익명 `moneybrief`, `<meta name="author">` 없음 |
| 규칙 2가 **가공 인물**("35세 직장인 A씨")을 의무화 | 구글이 요구하는 1인칭 실경험의 정반대 |
| 홈 첫 화면 보일러플레이트 | 홈 8개 글 발췌문이 **전부 동일 문구로 시작**(실측 확인) |

### ⚠️ 분량에 대한 정정 (중요)

초기에 조사 에이전트의 **5개 표본**을 근거로 "본문 평균 1,916자"라고 판단했으나,
**107개 전수 실측 결과 평균 2,410자**였다. 표본이 우연히 짧은 글에 쏠린 것.

```
1000~1500자:   5개  █████
1500~2000자:  20개  ████████████████████
2000~2500자:  45개  █████████████████████████████████████████████
2500~3000자:  23개  ███████████████████████
3000자~   :  14개  ██████████████
평균 2,410자 / 최소 1,326자 / 최대 4,557자 / 1,000자 미만 0개
```

→ **thin content는 실재하지만 완만하다.** 거절의 주된 축은 분량보다
**획일성 + E-E-A-T + 홈 첫인상**이다. 이 우선순위를 뒤집지 말 것.

### 사용자 확정 방침
- 콘텐츠: **핵심 40개 심화 + 약한 글 비공개**(삭제 아님, 되돌릴 수 있음)
- 작성자: **필명 + 운영 원칙·경력 명시** (실명은 비공개 유지)

---

## 2. Phase 0 — 완료 (2026-08-06)

| 작업 | 결과 |
|---|---|
| 약한 글 비공개 | **10개** 전부 403/404 확인 |
| 인바운드 카드 재연결 | **13개** 재발행, 점검 문제 0건 |
| 죽은 카드 제거 | `/68`이 삭제된 `/67` 링크 중이던 것 교체 |
| 인덱스 정리 | 108 → **96개** |
| 카드 건전성 전수 검사 | 카드 대상 76개 **전부 200 OK**, 비공개 글 링크 0건 |

**비공개 전환한 10개**
- thin(<1,500자): `/92`(1326) `/96`(1330) `/102`(1356) `/103`(1399) `/82`(1445)
- 주제 이탈(AI 도구): `/35` `/36` `/38` — 금융 블로그 토픽 권위 희석
- 중복: `/120`←`/106`이 포괄, `/66`←`/63`이 포괄

**인덱스 중복 등재 2건 제거**: `/37`(라이브와 다른 오제목), `/49`(슬러그 URL 판)

커밋: `b6f62e8 fix: 애드센스 Phase 0 — 약한 글 10개 비공개, 인덱스 96개로 정리`

### Phase 0에서 배운 함정
- **AI 글 3개는 인덱스에 슬러그 URL로 저장돼 있어** 숫자 URL 패턴(`tistory.com/(\d+)$`)에
  안 걸렸다. 1차 실행에서 놓쳤고 검증 로직도 같은 맹점이라 통과해버렸다.
  → **인덱스를 다룰 땐 숫자 URL과 슬러그 URL을 항상 둘 다 검사할 것.**
- 비공개 전환 **전에** 반드시 인바운드 카드를 재연결해야 한다(비공개 글 카드는 403).
- `update_posts.py`는 `/21`에서 Playwright `goto` 타임아웃이 **재현성 있게** 발생한다.
  대량 배치는 1회 재시도를 전제로 돌릴 것(재시도하면 나머지가 정상 처리됨).

---

## 3. 남은 작업

### Phase 1 — 하네스 규칙 수술 ✅ 완료 (2026-08-06)

**작업 중 결정적 발견**: 이번 세션에서 발행 전마다 손으로 고쳐야 했던 금지 문구들이
**전부 규칙 11·12·14의 예시 문장으로 하드코딩돼 있었다.** 에이전트가 예시를 그대로 복사한 것.
획일성의 작동 메커니즘을 현장에서 확인했다.

> "저라면 이 글은…" "결론부터 말씀드리면…" "바뀐 부분은 딱 하나예요"
> "결국 핵심은 하나예요" "(이 부분이 사실 제일 헷갈리는 포인트거든요)"
> → 전부 blog-writer.md 안에 **복사 가능한 예시**로 존재했음

| 대상 | 변경 내용 |
|---|---|
| 산출물 포맷 `## 마무리` | **리터럴 삭제** → 주제어 포함 고유 표현 의무 |
| 규칙 2 인물 사례 | **가공 인물("A씨") 금지** → 조건 기반 계산(형태 A) 또는 운영자 검증 경험(형태 B) |
| 규칙 5 체크리스트 | 전 포스트 의무 **폐지** → 절차·서류·자격요건일 때만, 배치당 1편 |
| 규칙 11 감정 주석 | 예시 문장 삭제 → "기능"만 설명 + 글 고유 내용 의무 |
| 규칙 12 훅 | **템플릿 문장 전면 폐지** → "담아야 할 정보"만 규정 + 금지 문구 9종 명시 |
| 규칙 14 결론 | "결국 핵심은 하나예요" 등 금지 상투구 추가 |
| **규칙 17 신설** | 본문 **3,000자+** / H2 **5~7개** / 섹션당 **400자+** / 마지막 H2 범용어 금지 |
| **규칙 18 신설** | E-E-A-T — 확인 경로 본문 명시 + 저자 박스 HTML |
| 메타 디스크립션 | "정리했습니다" 종결 **금지**, 글 고유 수치·결론 포함 |
| 제목 패턴 | `확인할 N가지`·`체크리스트`·`완전 정리`·em dash·`~할까?` 배치당 각 1편 |
| 📅/⚠️ 블록쿼트 | 본문 최상단 → **도입부 첫 문단 다음**, ⚠️ 문구도 주제별로 다르게 |
| 규칙 5 가독성 예시 | 예시 문장이 금지 문구였던 것 교체 + "형식만 참고" 경고 추가 |

**동기화 완료**: `.claude/CLAUDE.md`, `.claude/agents/quality-reviewer.md`
(분량·구조 / E-E-A-T / 메타·제목 반복 검증 3개 섹션 신설, 금지 훅 문구 9종 자동 검출 항목 추가)

> ⚠️ **다음 세션 주의**: 이 문서나 규칙 파일의 **예시 문장을 글에 그대로 쓰지 말 것.**
> 같은 실수가 4번 반복됐다. 예시는 형식·기능 설명용이다.

### Phase 2 — 홈 첫인상 수정 ✅ 완료 (2026-08-06)

`_workspace/adsense_final/move_notice.py` 작성 → 96개 전수 패치 → `update_posts.py`로 라이브 반영.
계획대로 **저자 박스(원래 Phase 4 예정)를 같은 배치에 동봉**해 재발행 1회로 두 가지를 끝냈다.

- 📅/⚠️ 공지 블록쿼트: 본문 최상단 → **도입부 첫 문단 다음**으로 이동
- 저자 박스 삽입: `글쓴이 머니브리핑 · 공식 기관 자료만 근거로 삼고...` + 소개 페이지 링크, 본문 최하단
- 96개 전부 성공(1차 시도에서 `/150`이 선행 `<hr>` 때문에 패턴 미매칭으로 누락 → 정규식에
  `(?:<hr[^>]*/?>\s*)?` 옵셔널 선두 매칭 추가해 해결, 총 96개 완료)
- 라이브 재발행 검증: 홈 8개 발췌문 **전부 서로 다른 문장으로 시작** (📅로 시작 0개, 실측 확인)
- 저자 박스 라이브 노출 스팟체크 3건 전부 확인

**부수 성과 — `update_posts.py` 안정화**: 로그인 직후 첫 `page.goto()`가 `networkidle` 대기에서
30초 타임아웃되는 현상이 이번 세션에서 **4번 재현**됐다(Phase 0의 `/21` 2회, Phase 2의 `/15` 2회 —
매번 포스트 번호는 다르고 "로그인 직후 첫 네비게이션"이라는 공통점). 수동 재시도 대신 근본 수정:
`goto`를 `networkidle` 시도 → 실패 시 `domcontentloaded`로 폴백하도록 try/except 추가.
**앞으로 대량 배치에서 이 현상이 사라질 것으로 기대** — 재발하면 이 문서에 추가 기록할 것.

커밋: 다음 커밋에서 함께 기록

**⚠️ 공지 위치 v1→v3 재변경 (2026-08-08, 사용자 확인)**: 위 v1("도입부 첫 문단 다음")을
실제 라이브에서 확인한 사용자가 두 차례 스크린샷으로 문제를 지적함 — v2("도입부 전체 다음/첫
H2 직전")도 여전히 도입부와 안내 불릿 사이에 끼어 부자연스러웠다. 최종적으로 사용자가 직접
제안한 **v3: 본문 맨 아래, 기존 마무리 면책 블록쿼트("⚠️ 이 글의 수치는...") 바로 위**로 확정.
`_workspace/adsense_final/move_notice_v3.py` 신규 작성, **96개 전체 재발행 완료**
(`.claude/CLAUDE.md`의 "금융 콘텐츠 팀 규칙" 섹션도 v3로 동기화됨). Phase 3에서 새로 심화하는
글은 `splice_deepen.py`가 blog-writer의 원본 위치(도입부 다음)로 접합하므로, **배치별로
`move_notice_v3.py` 재적용이 필요** — 배치 A~D 전부 이 단계를 거쳐 최종 반영함.

### Phase 3 — 핵심 40개 중 우선순위 20개 심화 ✅ 완료 (2026-08-07~2026-08-08)

**우선순위 20개 확정** (Phase 0 비공개 10개 제외 후, 현재 96개 중 최소 분량 순으로 재산출 —
`/37`은 비공개분 정리 과정에서 중복 등재 제거된 자리라 빠지고, `/63,/153,/78`을 채워 20개 확정):

```
61,95,64,85,97, 99,83,62,126,141, 29,31,73,98,81, 74,28,153,78,63
```

4개 배치(A~D, 배치당 5개)로 나눠 처리. **배치당 blog-writer 서브에이전트 5개를 병렬 실행**하고,
에이전트에게는 본문만 맡긴다 — 카드·출처표·저자박스는 신규 스크립트
`_workspace/adsense_final/splice_deepen.py`로 **라이브에 이미 검증된 것을 그대로 재사용**해
접합한다 (카드 fabrication은 이 세션에서 반복 확인된 실패 패턴이라 애초에 에이전트에게 안 맡김).

심화 방법 — 단순 증량이 아니라 thin의 원인 제거:
- 섹션 수를 줄이고(7~10 → 5~7) 각 섹션을 400자 이상으로
- 표마다 해석 문단 2~3문장, 규칙 15의 3요소 중 2개 이상 실제 충족
- 규칙 16 공식 통계를 기준일과 함께 최소 1개(WebSearch 확인)
- 가공 인물 → 조건 기반 계산 또는 운영자 검증 경험으로 교체

**배치 A (61,95,64,85,97) — ✅ 완료, 라이브 반영·검증까지 끝남**
- 5개 전부 심화 → 검증 → 접합(`splice_deepen.py`) → `update_posts.py` 재발행 →
  라이브 실측(3,458~3,975자, 전부 3,000자 기준 초과) 완료
- **`/61`(청년미래적금) 작성 중 에이전트가 원본 라이브 글의 팩트 오류를 발견**: 만기
  5년→실제 3년, 월납입한도 70만원→실제 50만원. WebSearch로 fsc.go.kr 공식 조건과
  독립 재검증해 일치 확인, 정정 반영함. **Phase 3가 단순 증량이 아니라 팩트체크 기회이기도
  하다는 걸 확인** — 원본 다수가 출시 전 예상 조건으로 쓰였다가 이후 조건이 확정되며
  괴리가 생긴 것으로 추정, 나머지 19개도 같은 패턴 주의.
- 검증 중 잡은 문제: `/95`("핵심은 하나예요"), `/83`("결국 숫자가 말해주는 건 하나예요")에서
  세션 금지 문구("결국 핵심은 하나예요" 등, blog-writer.md 규칙 12/14 참조)의 **근접 변형**을
  발견해 수정. 규칙에 정확히 그 문장을 금지해도, 조사·어미만 바꾼 변형은 새어나올 수 있다는 뜻 —
  검증 시 반드시 "결국 ~은/는 하나예요" 패턴 자체를 재검사할 것.

**배치 B (99,83,62,126,141) — ✅ 완료, 라이브 반영·검증까지 끝남**
- `/141`(개인사업자 공동대출) — **원본 팩트 오류 발견**: 분담비율 60:40→실제 50:50,
  "이미 신청 가능"처럼 서술돼 있었으나 실제로는 **2027년 출시 목표인 미출시 상품**
  (2026-07-09 금융위 발표 기준). WebSearch 독립 재검증 완료, 정정 반영.
  근접 금지문구("이 글에서 먼저 봐야 할 건 딱 하나예요")도 수정 완료.
- `/83`("결국 숫자가 말해주는 건 하나예요" → 수정), `/99`·`/126`·`/62`는 검증 통과, 이상 없음.
- 5개 전부 `splice_deepen.py` → `update_posts.py` → `move_notice_v3.py` → 재발행 → 라이브 실측 완료.

**배치 C (29,31,73,98,81) — ✅ 완료, 라이브 반영·검증까지 끝남**
- `/73`(자동차보험 5부제) — 에이전트가 **정책의 규제 근거 자체가 시행 몇 주 뒤 폐지**됐음을
  발견, WebSearch 독립 재검증으로 정확성 확인·반영.
- `/73`은 라이브 원본에 "함께 읽으면 좋은 글" 카드 섹션이 없는 구버전 포맷이라
  `splice_deepen.py`의 `TAIL_RE`가 매칭 실패 → **`FALLBACK_TAIL_RE`**(⚠️ 마무리 면책
  블록쿼트 기준 분할) 신설해 해결. 이후 같은 케이스(`/74`, `/78`)에도 재사용됨.
- 근접 금지문구 다수 발견·수정: `/98` 3건("여기서 [논리 체인/의사결정 분기/역설적인
  지점]이 생깁니다/나옵니다" — 규칙에 없던 **새 패턴**, 이후 배치 프롬프트에 명시적으로 추가),
  `/31`("이 글에서 먼저 확인해야 할 건 ~입니다").
- 5개 전부 재발행·`move_notice_v3.py` 적용·라이브 실측 완료.

**배치 D (74,28,153,78,63) — ✅ 완료, 라이브 반영·검증까지 끝남**
- `/74`(인버스 ETF) — "여기서 역설적인 지점이 나옵니다"(배치 C에서 새로 발견한 자기지시적
  패턴, 배치 D에서도 재발 확인) → "역설적이게도,"로 수정. `/78`(대출금리 산정방식)도
  "여기서 역설이 생깁니다" + "정리하자면"(금지 문구 "정리하면,"의 활용형 변주) 2건 수정.
  `/28`(보험 리모델링), `/63`(단일종목 레버리지), `/153`(자동차세 연납)은 검증 통과, 이상 없음.
- 5개 전부 `splice_deepen.py` → 1차 발행(라이브 실측 12,001~12,694자, 카드·출처표 포함) →
  `move_notice_v3.py`로 공지 하단 재배치 패치 생성 → 2차 재발행 → 라이브에서 공지가
  첫 H2 뒤(하단)로 이동했음을 정규식으로 재확인, 전부 통과.

**배치 E~H (37,140,115,26,75 / 89,128,149,25,90 / 77,101,33,142,136 / 122,30,121,59,145)
— ✅ 완료, 라이브 반영·검증까지 끝남 (2026-08-10, 나머지 20개)**

- 원 계획의 "핵심 40개" 중 배치 A~D(20개, 2026-08-07~08)에 이어 나머지 20개를 이 세션에서
  마저 완료 — **핵심 40개 전부 심화 완료.**
- **반복 발견된 새 실패 패턴**: 이번 20개에서는 에이전트의 **H2 개수 자체 보고가 신뢰 불가**
  하다는 점이 두드러졌다 — "H2 6~7개"라고 자체 보고한 글 중 다수가 실제로는 8~12개였다
  (섹션을 잘게 쪼개는 습관 때문에 자기 카운트가 부정확). 검증 없이 그대로 발행했다면
  thin content(섹션당 200~300자)가 대량 재발됐을 것 — **자체 보고를 신뢰하지 말고 매번
  `grep "^## "`로 직접 재검증하는 습관이 실제로 여러 건을 잡아냄** (예: `/115` 12개→7개,
  `/128` 11개→7개, `/149` 11개→7개 등 대규모 통합 필요했던 사례 다수).
- **유사 주제 글 간 중복 방지**: 이번 20개 중 여러 건이 이미 심화한 글과 주제가 겹쳤다
  (`/33` vs `/25` 실손보험 세대, `/122` vs `/128` 전세보증보험, `/121` vs `/115` 안심상속
  원스톱서비스, `/142` vs `/140` 원천세/간이지급명세서). 에이전트 프롬프트에 "이미 발행한
  글과 어떻게 다른 각도로 쓸지" 명시적으로 지정해 중복 콘텐츠를 사전에 방지함.
- 근접 금지문구 추가 발견: `/25`(0개로 시작해 새로 추가 필요), `/33`("~정리하면" H2 발견·수정),
  `/128`·`/149`(마지막 H2 "~정리" 계열 재발), `/121`("~순서대로 정리하면" 재발) — "정리"로
  끝나는 헤더/문장이 규칙에 없던 새로운 새어나옴 패턴으로 확인됨.
- **인덱스 정합성 문제 재발견**: `/37,/26,/25,/90,/33,/30` 6개가 실제 라이브 존재에도
  `_posts_index.md`에 누락돼 있었다(기존에 문서화된 "알려진 한계"와 동일 패턴). 이번에
  전부 발견해 `thumb_host.py`로 영구 썸네일 확보 후 인덱스에 추가 완료.

**Phase 3 최종 종합**: **핵심 40개 전부** 라이브 반영·`verify_post.py` 자동 점검 통과·
공지 v3 위치 확인 완료. 검증 과정에서 반복 발견된 근접 금지문구 패턴(자기지시적
"여기서 ~이 생깁니다/나옵니다" 계열, "정리하자면"/"~정리하면"류 활용형 변주)은 명시적
금지 문구를 늘려도 활용형·유의어 변형은 매번 새로 새어나왔다 — 근본 해법은 아직 규칙만으로는
막히지 않으며, **발행 전 매번 직접 재검증하는 절차 자체가 유일하게 효과가 검증된 방어선**
이라는 점을 기록해 둔다.

### Phase 4 — E-E-A-T ✅ 자동화 가능 부분 완료 (2026-08-11)
- [x] 글마다 저자 박스 — ✅ Phase 2에서 96개 전부 완료
- [x] `/page/머니브리핑-소개` 강화 — 575자 → **1,315자**. "이 블로그를 시작한 이유"(전세보증보험
      비교 정보를 직접 찾아 헤맨 경험), "자료를 확인하는 방법"(공식 자료 우선·기준일 표시·오류
      수정 절차 구체화), "다루지 않는 것"(특정 상품·종목 추천 안 함, 개인 맞춤 자문 아님) 신설.
      **가짜 경력·자격증은 절대 기재하지 않음** — 실명 비공개 방침과 정직성을 지키기 위해
      검증 가능한 "운영 원칙과 절차"만 신뢰 신호로 사용(허위 E-E-A-T는 발각 시 더 큰 리스크).
      발행: `_workspace/adsense_final/update_about_page.py`(page/17, TinyMCE setContent 재사용).
- [x] `/page/이용약관` 신규 생성 완료(기존 404 → 200). 콘텐츠 성격·정보 한계·책임·광고 제휴
      표시·저작권·문의 7개 섹션. 발행 URL: `https://j2gblog.tistory.com/pages/이용약관`(내부 ID 155).
      생성 스크립트: `_workspace/adsense_final/create_terms_page.py` — **새 페이지 제목 입력은
      JS `value` 세팅이 무시됨(React 컨트롤 입력), `focus()` 후 `page.keyboard.type()` 필요**
      (기존 태그 입력 이슈와 동일 패턴, CLAUDE.md "태그 입력" 섹션 참조). "공개" 라디오도
      posts와 동일하게 TreeWalker로 텍스트 노드 탐색 후 클릭해야 함.
- [ ] `<meta name="author">` — **사용자가 직접** 티스토리 스킨 편집기에서 추가해야 함(자동화 불가)

### Phase 5 — 최종 점검 후 검토 요청 (2026-08-11 진행 중)
- [x] 홈 8개 발췌문이 서로 다른가 — 8/8 고유, 📅로 시작하는 것 0개 확인
- [x] 표본 10개가 전부 3,000자+인가 — Phase 3(핵심 40개) 전수 확인 완료
- [x] `## 마무리`로 끝나는 글이 없는가 — 2026-08-14 65개(58%) 발견 후 85개 배치(g1~g4) 전량
      수정 완료. **2026-08-17 라이브 115개(신규 /160·/161 포함) 재스캔 — `## 마무리`·
      `개인적으로는`·`이런 분께 해당합니다`류 전부 0건, 전량 clean.**
- [x] desc "정리했습니다" 종결 30% 미만인가 — 라이브 109개 전수 스캔, **0%**
- [x] 소개·문의·개인정보처리방침·이용약관 4개 전부 200인가 — 2026-08-11 확인 완료
- [x] 빈 카테고리 0개인가 — **2026-08-17 재확인**: `소프트웨어 비교`(5개 글)·`AI 도구활용`
      (8개 글) 둘 다 그사이 발행분으로 채워져 더 이상 비어있지 않음. 2026-08-06~11 시점
      기록은 stale했던 것으로 확인, 조치 불필요.
- [x] `verify_post.py` 전수 무결 — **1차 스캔에서 109개 중 13개가 공지·면책
      블록쿼트 자체가 없는 것으로 발견, 전부 수정·재발행·검증 완료** (아래 상세)
- [x] `refresh_thumbs.py --check` — **2026-08-17 실행**: 만료 0 / 7일내 만료예정 0 / 정상 0.
      thumb_host.py(jsdelivr 영구 CDN) 도입 이후 daumcdn 서명 URL 자체가 인덱스에 없어
      이 스크립트의 검사 대상이 구조적으로 사라짐 — 정상이며 더 이상 의미 있는 체크가 아님.

**🔴 규칙 리터럴 획일성 — 같은 함정 세 번째 (2026-08-14, 사용자 지적으로 발견)**

사용자가 "최근 발행 글 3개의 훅 패턴이 전부 `이런 분께 해당합니다`로 동일한데
스크립트가 고정하는 거냐"고 물어 조사한 결과, **발행 스크립트가 아니라
`blog-writer.md` 규칙 13**이 원인이었다. 규칙 13이 이 문구를 **"필수 형태"로
코드블록에 담아둔** 탓에 에이전트가 불릿만 채우고 문장은 통째로 복사해 왔다.

**실측 확산률 (라이브 113개, 수정 전)**

| 리터럴 | 출처 | 보유 |
|---|---|---|
| `## 마무리` | 구 산출물 포맷 | 65개 (58%) |
| `개인적으로는` | 규칙 3 예시 문장 | 64개 (57%) |
| `이런 분께 해당합니다` | **규칙 13 "필수 형태"** | 50개 (44%) |

가장 심각한 건 세 번째다 — 이 블록이 **도입부**에 있어서 Tistory 홈 목록
발췌문이 그대로 긁어갔고, **홈 첫 화면 8개 중 5개에 같은 문구가 노출**되고 있었다.
📅 공지를 하단(v3)으로 옮겨 해결했던 "홈 첫인상 보일러플레이트"가 **다른 문구로
재발**한 것이다.

**조치 1 — 규칙 수술 (재발 차단)**
- 규칙 13: "필수 형태" 코드블록 삭제 → 기능만 규정("독자가 자기 상황과 대조할
  구체적 조건 제시 + 멈춰도 된다는 신호"), 과포화 문구 3종 명시적 금지, 형식(불릿/문장)도
  글에 맞게 선택하도록 개방
- 규칙 3: `개인적으로는` 문두 금지 + 문장 구조 자체를 매번 다르게 쓰도록 재작성
- `.claude/CLAUDE.md`에 **"규칙 파일에 완성 문장을 코드블록에 넣지 말 것"** 원칙과
  실측 확산률을 박아둠 — 네 번째 재발 방지가 목적

**조치 2 — 소급 수정 (사용자 선택: 홈 노출분 우선 ~25개)**
최근 25개(`134~159`)에 대해 `_workspace/adsense_final/fix_literals.py` 작성.
**교체 문구를 스크립트가 생성하지 않는 것이 핵심** — 자동 생성하면 또 다른 획일성이
생기므로 사람이 글마다 지정한 맵만 적용하고, 맵에 없으면 건드리지 않고 경고만 낸다.
3배치(9+8+8)로 발행, 25개 전부 `verify_post.py` 통과.

**결과**: 대상 25개 리터럴 잔존 **0건**, **홈 발췌문 오염 5/8 → 0/8**.
사이트 전체는 마무리 65→51, 개인적으로는 64→46, 이런분께 50→34로 감소.

**조치 3 — 잔여 전량 처리 (2026-08-15 완료)**
남은 85개(`13~133`)를 4개 그룹으로 나눠 같은 방식으로 처리했다
(`fix_literals_g1~g4.py`, 각 파일에 글별 교체 문구를 사람이 직접 지정).

**최종 결과 — 라이브 113개 전수 재검증에서 네 리터럴 모두 0건**

| 리터럴 | 수정 전 | 수정 후 |
|---|---|---|
| `## 마무리` | 65개 (58%) | **0개** |
| `개인적으로는` | 64개 (57%) | **0개** |
| `이런 분께 해당합니다` | 50개 (44%) | **0개** |
| `이런 분은 건너뛰셔도 됩니다` | 50개 (44%) | **0개** |

작업 중 발견한 것들:
- `/64`는 `개인적으로는`이 괄호 감정 주석 안에 있어("이 부분이 개인적으로는…")
  단순 치환 시 "이 부분이 여기가"로 문장이 깨졌다 — 발행 전 dry-run에서 잡아 수정.
- `/81`·`/85`·`/64`는 한 글에 `개인적으로는`이 2회 등장했다. 잔존 검사가 이를 잡아냄.
- **`/100`(국민연금 보험료 인상)에는 규칙 3의 예시 문장이 통째로 복사돼 있었다** —
  "출시 후 최종 조건을 한 번 더 확인한 뒤 결정하는 게 낫다"는 문장이 국민연금
  인상 글에 붙어 주제와 맞지도 않았다. 리터럴 복사가 내용 오류로까지 번진 사례라
  문장 자체를 주제에 맞게 다시 썼다.
- 발행 후 경고로 뜬 카드 이미지 실패는 전부 기존 daumcdn 서명 만료 건으로 이번 작업과 무관.

**규칙 재발 방지 강화 (2026-08-14)**
`blog-writer.md` **최상단에 "🚫 0순위 — 금지 리터럴" 섹션 신설** — 이 문서의 어떤
규칙보다 우선한다고 명시하고, 금지 리터럴 표(발생 원인·실측 오염도 포함)와
"이 문서의 예시 문장은 형식 참고용이지 복사 대상이 아니다"는 원칙, 그리고
"다른 주제 글에 붙여도 말이 되면 템플릿이므로 실격"이라는 판별법을 넣었다.
`quality-reviewer.md`에도 같은 목록을 검증 항목으로 반영해 **발행 전에 걸러지도록** 했다.

**🔴 새로 발견 — `_posts_index.md` 누락이 만든 컴플라이언스 사각지대 (13개)**

`verify_post.py`로 라이브 109개 전수 스캔한 결과 `/44,45,46,47,48,50,51,52,53,54,55,56,58`
13개가 **`<blockquote>` 자체가 0개** — 즉 📅 공지·⚠️ 면책 문구·저자박스가 전혀 없는
상태였다. 원인은 이 13개(및 공지 있는 9개 포함 총 22개)가 애초에 `_posts_index.md`에
등재된 적이 없어서, Phase 2의 대량 공지 삽입 작업(`move_notice.py`, 인덱스 순회 방식)이
처음부터 이 글들을 건드리지 못했기 때문 — 새로 생긴 문제가 아니라 프로젝트 초기부터
누적된 사각지대였다.

**조치**: `_workspace/adsense_final/fix_missing_compliance.py` 신규 작성 — 카드·구버전
출처표는 이미 있었으므로, "함께 읽으면 좋은 글" H2 직전에 📅/⚠️ 공지+면책 블록쿼트를
삽입하고(11개는 금융 강화판, `/45`·`/47`은 AI 도구 주제라 일반판 면책 + AI 가격 안내
문구), 맨 끝에 저자박스를 추가했다. 13개 전부 재발행·`verify_post.py` 재검증 통과.
`thumb_host.py`로 영구 썸네일 확보 후 `_posts_index.md`에 13개 신규 등재 완료
(카드 이미지 만료 경고 일부 남아있는데, 이건 별개의 기존 daumcdn 서명 만료 문제 —
"[내부 링크 카드] 썸네일 URL 만료" 섹션 참조, 이번 수정 범위 아님).

**⚠️ 아직 처리 안 됨 — 인덱스 누락 22개 중 나머지 9개**: `23,24,27,28,29,31,32,34,40`은
공지·면책 블록쿼트는 이미 있어(verify_post 통과) 급하지 않지만, 여전히 인덱스에
없어서 내부링크 카드 후보에서 빠져 있다. 다음 세션에서 마저 추가할 것.

**✅ 해결 완료 — Phase 3 심화 40개 중 24개 공지·면책·출처표 중복 (2026-08-11)**

`/128`을 스팟체크하다가 "공식 출처" 섹션이 2개 있는 걸 발견 → 핵심 40개 전수
재검사 결과 **24개**에서 공지(📅)·면책(⚠️)·출처표 중 하나 이상이 중복돼 있었다.

원인: `splice_deepen.py`의 `TAIL_RE`가 "함께 읽으면 좋은 글" H2 **이후 전체**를
통째로 붙여넣는데, 심화 대상이었던 **원본 포스트가 이미 자체적으로 공지·면책·
출처표를 갖고 있었던 경우**(카드 섹션 뒤에 구버전 스타일로 붙어 있었음), 에이전트가
새로 쓴 공지·면책(올바른 v3 위치)과 원본의 낡은 것이 그대로 중복 삽입됐다.

**조치**: `_workspace/adsense_final/dedupe_compliance.py` — 컴포넌트(notice/disclaimer/
cards/출처표 신구스타일/쿠팡공시/저자박스)를 정규식으로 전부 원위치에서 제거한 뒤,
가장 내용이 긴(=온전한) 것만 골라 카드 직전에 공지+면책, 맨 끝에 출처표+저자박스로
재조립하는 방식.

**⚠️ v1 설계 결함 발견·수정**: 최초 버전은 "첫 notice 이전을 본문으로 간주"하는
위치 가정 방식이었는데, `/62`가 notice를 도입부 바로 뒤(v1/v2 시절 옛 위치)에 갖고
있어서 그 이후 본문 H2 전체(약 8,000자)가 통째로 날아가는 사고 직전까지 갔다 —
발행 전 각 포스트의 정리 후/정리 전 글자수 비율을 출력해 급감(70% 미만)을 자동
탐지하는 안전장치를 넣은 뒤에야 발견했다. **본문 손실 없이 컴포넌트만 제거**하는
v2 방식으로 전면 재작성 후 24개 전부 처리.

**결과**: 24개 전부 재발행 → 라이브 재검증(신규/구버전 출처표 합계 ≤1, 면책 정확히 1,
공지 정확히 1, 저자박스 정확히 1) **전부 통과**, `verify_post.py` 24개 전부 통과.
분량 감소는 0~11% 수준(중복 블록만 빠진 정상 범위), 본문 H2 손실 0건.

---

## 4. 사용자가 직접 해야 하는 것 (티스토리 관리자)

계정 설정이라 자동화 불가.

1. **빈 카테고리 삭제** — `소프트웨어 비교`(0개), `AI 부업, 수익화`(0개), `AI 도구활용`(글 전부 비공개됨)
2. **메뉴 카테고리 확대** — 현재 홈 nav 4개(보험비교/절세·연금/대출·금리/주식,ETF) → **신용카드**, **예금·금리** 추가
3. **사이드바 빈 "공지사항" 위젯** 제거
4. **푸터에 소개·개인정보처리방침 링크 추가** (현재 `TISTORY`/`관리자` 2개뿐)
5. 마지막에 애드센스 **검토 요청** 클릭

### ~~ads.txt 등록~~ — 불필요 (2026-08-06 정정)
- `tistory.com/ads.txt`는 이미 존재. **서브도메인 블로그는 루트에 파일을 놓을 수 없다**
  (스킨 편집기 업로드는 `/images/` 경로라 스펙이 요구하는 루트가 아님).
- **ads.txt는 승인 심사 항목이 아니다.** 승인 후 "수익 손실 위험" 경고로 뜨는 항목.
  거절 통지에도 사유는 "가치가 별로 없는 콘텐츠" 하나뿐.
- → 승인 전까지 무시. 승인 후 티스토리 관리 → 수익 메뉴의 애드센스 연동으로 처리.

---

## 5. GSC 색인 오류 — 이번 범위에서 제외 (판단 근거)

스크린샷의 GSC 수치는 대부분 애드센스 거절과 무관하다. 다시 조사하느라 시간 쓰지 말 것.

- **"적절한 표준 태그가 포함된 대체 페이지" 117개** — `/m/74` 등 모바일 URL.
  Tistory 자동 생성이고 canonical이 숫자 URL을 가리키는 **정상 동작**
  (2026-07-17 조사에서 확인, `.claude/CLAUDE.md`에 기록됨). 조치 불필요.
- **404 12개** — 삭제된 글 23개(`/67` 등) 잔재. 시간이 지나면 자연 정리.
  단 내부 링크가 이들을 가리키면 안 됨 → Phase 0에서 `/68` 1건 처리 완료.
- **robots.txt 차단 4개** — `/manage` 등 관리자 페이지. 정상.
- 다만 sitemap이 `/153`과 `/m/153`을 **둘 다 제출**하는 건 중복 신호 — 여유되면 확인.

---

## 6. 도구 사용법

| 스크립트 | 용도 |
|---|---|
| `_workspace/adsense_final/select_weak.py` | 라이브 전수 본문 실측 + thin/중복/주제이탈 후보 선별 (읽기 전용) |
| `_workspace/adsense_final/redirect_before_private.py` | 비공개 예정 글의 인바운드 카드를 같은 카테고리 충실한 글로 자동 재연결 |
| `_workspace/adsense_final/clean_index.py` | 인덱스에서 비공개 글·중복 등재 제거 |
| `_workspace/adsense_final/move_notice.py` | (Phase 2에서 사용 완료) 📅/⚠️ 공지를 도입부 다음으로 이동 + 저자박스 삽입 |
| `_workspace/adsense_final/splice_deepen.py` | **Phase 3 진행 중 사용** — `deepen_{id}.md`(에이전트 심화 본문)를 라이브의 카드·출처표·저자박스와 접합해 `enhanced_{id}.html` 생성. `python splice_deepen.py <id> [id...]` |
| `_workspace/dedup/set_private.py` | 지정 포스트 비공개 전환 (`TARGETS` 수정 후 실행) |
| `update_posts.py` | `enhanced_{id}.html`을 라이브에 반영 (`UPDATE_POSTS` 수정 후 실행) |
| `verify_post.py` | 발행 후 자동 점검 (메타 노출·블록쿼트·카드 이미지·슬러그 불일치) |
| `thumb_host.py` | **신규 발행 시 필수** — 썸네일 영구 호스팅(jsdelivr) |
| `refresh_thumbs.py --check` | 썸네일 만료 점검 (영구 호스팅 전환 후엔 거의 불필요) |

> ⚠️ `_workspace/`는 `.gitignore` 대상이라 위 스크립트들은 **커밋되지 않는다.**
> 다른 PC에서 이어서 작업하려면 재작성이 필요하다. 이 문서의 설명만으로 재작성 가능하도록 유지할 것.

**썸네일 영구 호스팅(2026-08-03 도입)**: 신규 발행 후 반드시 `thumb_host.py`로
썸네일을 `assets/thumbnails/{id}.jpg`에 저장하고 **커밋·푸시**해야 jsdelivr가 서빙한다.
예전 daumcdn 서명 URL 방식으로 되돌아가지 말 것(계정 단위 월 1회 일괄 만료됨).
