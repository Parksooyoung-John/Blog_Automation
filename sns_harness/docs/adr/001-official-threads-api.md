# ADR-001: 공식 Threads API 사용

- 상태: 승인
- 결정: 브라우저 자동화가 아니라 `graph.threads.com`의 컨테이너 생성·게시 API를 사용한다.
- 이유: UI 변경과 로그인 세션에 덜 취약하고 게시 ID·사용 한도·오류를 구조적으로 처리한다.
- 결과: v1은 단일 계정 장기 토큰을 Secret으로 받고 OAuth 서비스는 만들지 않는다.
